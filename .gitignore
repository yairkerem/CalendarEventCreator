# Deliberately kept out of this public repo.
#
# KICKOFF.md names the kids. calendar-backend.gs used to as well, in its config
# block -- that block now lives in Script Properties, one deployment per
# household, so the backend itself is tracked here and shared.
KICKOFF.md
.claude/
